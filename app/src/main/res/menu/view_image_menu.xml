<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/favourite"
        android:icon="@android:drawable/star_big_off"
        android:title="Favourite"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/shareNote"
        android:icon="@android:drawable/ic_menu_share"
        android:title="Share Note"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/exportNote"
        android:icon="@android:drawable/star_big_off"
        android:title="Backup" />


    <item android:id="@+id/remove"
        android:icon="@android:drawable/star_big_off"
        android:title="Remove Note"
        app:showAsAction="never"/>

    <item android:id="@+id/delete"
        android:icon="@android:drawable/ic_menu_delete"
        android:title="Delete"
        app:showAsAction="never"/>




</menu>